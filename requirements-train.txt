# GPU/CUDA training stack — install on a machine with an NVIDIA GPU.
# (Colab/RunPod/local RTX). Not needed for data prep, eval, or the test suite.
-r requirements-core.txt
torch>=2.3.0
transformers>=4.44.0
peft>=0.12.0
trl>=0.9.0
datasets>=2.20.0
bitsandbytes>=0.43.0
accelerate>=0.33.0
wandb>=0.17.0
